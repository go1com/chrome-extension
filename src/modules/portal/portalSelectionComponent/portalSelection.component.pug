select.form-control([(ngModel)]="portal", (change)="onPortalChanged()", [disabled]="disabled")
  option(value="") {{ placeholder }}
  option(*ngFor="let option of availablePortals", [value]="option.id") {{ option.title }}
